apiVersion: v1
kind: Service
metadata:
  name: {{ include "fbg.fullname" . }}
  labels:
    {{- include "fbg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fbg.selectorLabels" . | nindent 4 }}
