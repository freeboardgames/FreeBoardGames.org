apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fbg.fullname" . }}-test-connection"
  labels:
    {{- include "fbg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fbg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
