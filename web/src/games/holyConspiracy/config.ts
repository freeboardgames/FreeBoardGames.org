import { IGameConfig } from 'gamesShared/definitions/game';
import { HolyConspiracy } from './game';
import { Board } from './board';

const config: IGameConfig = {
  bgioGame: HolyConspiracy,
  bgioBoard: Board,
};

export default config;
