Assumptions about game state:

- Javascript Object with specific fields:
  - status: Either 'ACTIVE' or 'FINISHED'
  - winner: If present, e-mail of the winner.
  - players: Array with emails of every player.
  - log: Array of objects containing:
    - state: Javascript object containing game state on that turn
      - i.e. Checkers contain loading, turn, which piece was selected and board.
    - action: Javascript object with state for each game action.
